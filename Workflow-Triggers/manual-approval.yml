name: Manual Approval Demo

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Build completed"

  deploy:
    runs-on: ubuntu-latest
    needs: build

    environment:
      name: PROD

    steps:
      - run: echo "Deploy approved"